# P2 Tasks (optional)

Diese Tasks sind nur umzusetzen, wenn audit2 sie als offen/partial markiert.

## P2-1 client_config remote
- Admin edit client_config
- Client poll interval, min_supported_version, update policy

## P2-2 Downloads/Knowledge Base persistence
- Wenn derzeit nur static → DB models + admin CRUD

## P2-3 Keycloak / Ops
- Realm export backup plan (script + docs)
- Client cleanup (remove legacy clients/scopes)

## P2-4 Rules Engine
- Nur wenn Spezifikation/DTOs im Repo vorhanden, sonst `unknown`.

→ Wenn umgesetzt: audit2 Matrix updaten und in `REPLY_TEMPLATE` reporten.
