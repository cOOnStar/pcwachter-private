# Release Notes pro Programm

Dies ist der zentrale Release-Ordner.

Programme:

- `docs/releases/gui/`
- `docs/releases/updater/`
- `docs/releases/service/`
- `docs/releases/api/`
- `docs/releases/console/`
- `docs/releases/webseite/`

Regel:
- Client-Programme (`gui`, `updater`, `service`):
  - Dateiname `vX.Y.Z.md`
- Server-Programme (`api`, `console`, `webseite`):
  - Dateiname als fortlaufende Zahl mit `v` (`v1.md`, `v2.md`, `v3.md`)
