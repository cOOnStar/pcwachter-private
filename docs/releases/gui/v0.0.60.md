# Release Notes v0.0.60

Release-Datum: 26.02.2026

## Highlights
- Release-Workflow repariert: `msbuild` wird auf GitHub Actions Runnern explizit eingerichtet.
- Native-Worker-Build im Release-Prozess läuft damit zuverlässig auf `windows-latest`.

## Technische Änderung
- `.github/workflows/release-installer.yml`
  - Neuer Step: `microsoft/setup-msbuild@v2` direkt nach .NET-Setup.

## Hinweis
- Dieses Release ist ein technischer Build-/CI-Hotfix ohne funktionale UI-Änderungen.
