# Release Notes v0.0.31

Release-Datum: 25.02.2026
Tag: `v0.0.31`

## KurzÃ¼berblick

- Installer-Artefakte sind jetzt einheitlich benannt:
  - `PCWaechter_live_installer.exe` (Downloader/Bootstrapper)
  - `PCWaechter_offline_installer.exe` (vollstÃ¤ndiger Offline-Installer)
- Release-Pipeline, Manifest und Bootstrapper-Logik wurden auf das neue Namensschema umgestellt.
- Die App-Struktur wurde konsolidiert (`desktop-app`, `system-service`, `api`).

## Installer und Update-Pfad

- NSIS-Setup und NSIS-Bootstrapper verwenden die neuen Artefakt-Namen.
- Inno Setup erzeugt den Offline-Installer unter dem neuen Zielnamen.
- `installer-manifest.json` sowie das Sample-Manifest wurden auf neue Download-/Asset-Namen aktualisiert.
- Der Bootstrapper verwendet nur noch das neue Namensschema fÃ¼r den Offline-Installer.
- Der Desktop-Updater lÃ¤dt gezielt `PCWaechter_offline_installer.exe`.

## CI/CD & Release-Automation

- Workflow `release-installer.yml` auf neue Dateinamen angepasst (Normalisierung, Signierung, Upload, Label-Patching, Cleanup).
- Build-Skripte fÃ¼r Installer/Bootstrapper (`scripts/build-installer.ps1`, `scripts/build-bootstrapper-nsis.ps1`) entsprechend aktualisiert.

## Doku

- `README.md` und `docs/07_release/00_release-process.md` auf das neue Installer-Namensschema aktualisiert.
- Bootstrapper-Doku prÃ¤zisiert.

## Migrationshinweis

Dieses Release enthÃ¤lt zusÃ¤tzlich Struktur-Migrationen innerhalb von `apps/*`:

- `apps/agent-wpf` â†’ `apps/desktop-app`
- `apps/agent-service` â†’ `apps/system-service`

FÃ¼r Nutzer der Installer sind primÃ¤r die neuen Artefakt-Namen und der aktualisierte Release-/Update-Pfad relevant.

