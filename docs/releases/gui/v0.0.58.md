# Release v0.0.58

Release-Datum: 26.02.2026

## Neu

- Neuer nativer Update-Worker unter `apps/native-updater-worker` (`PCWachter.UpdateWorker.exe`).
- Release-Workflow enthÃ¤lt Preflight-Checks fÃ¼r native Toolchain (`msbuild`) und Publish-Verifikation der Worker-EXE.

## Verbessert

- Installer/Scheduler auf Chrome-Ã¤hnlichen Hintergrundmodus umgestellt:
  - Task: `PCWachter Update`
  - Aufruf: `PCWachter.UpdateWorker.exe --silent --scheduled`
- Build-Pipeline fÃ¼r den Installer baut den nativen Worker und Ã¼bernimmt ihn in den Publish-Output.

## Entfernt / Bereinigt

- alte UI-Background-Updatepfade entfernt (`--background-update-check`).
- Alte Task- und Worker-Namen aus GUI/Core/Installer entfernt.
- Veraltetes .NET-Worker-Projekt `apps/desktop-updater-worker` entfernt.

## Hinweise

- FÃ¼r lokale Installer-Builds werden Visual Studio Build Tools (C++/MSVC inkl. `msbuild`) benÃ¶tigt.

