# Release Notes v0.0.59

Release-Datum: 26.02.2026

## Highlights
- Updater-Manifest auf strikt neues Schema vereinheitlicht (`version`, `installer.url`, `installer.sha256`, `installer.silentArgs`).
- Native Update-Worker verarbeitet nur noch das neue Manifest-Schema (keine alten Fallbacks).
- Release-Workflow generiert `installer-manifest.json` ausschlieÃŸlich im neuen Schema.

## Technische Ã„nderungen
- `apps/native-updater-worker/main.cpp`
  - Altes Parsing entfernt.
  - Regex-Literale und Version-Vergleich robust gemacht.
- `.github/workflows/release-installer.yml`
  - Manifest-Erzeugung auf neue Felder umgestellt.
  - SHA256 wird vom Offline-Installer berechnet und eingetragen.
- `release/installer-manifest.json`
  - Lokales Referenzmanifest auf neues Schema umgestellt.
- `apps/installer-bootstrapper/installer-manifest.sample.json`
  - Beispielmanifest auf neues Schema umgestellt.

## Hinweis
- Dieser Hotfix ist bewusst "new-only" und Ã¼bernimmt keine Altlasten-/alte Strukturen.

