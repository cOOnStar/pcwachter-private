# Release Notes v0.0.55

Release-Datum: 26.02.2026
Tag: `v0.0.55`

## Kurzüberblick

- CI-Build für Installer stabilisiert.
- Sprachressourcen-Filter im Publish auf eine robuste Konfiguration angepasst.
- Installer-Release-Pipeline läuft wieder ohne MSBuild-Parameterfehler.

## Build / CI

- `SatelliteResourceLanguages` wird im Setup-Build stabil auf `de` gesetzt.
- Fehlerbild `MSB1006 Property is not valid (Switch: en)` wurde damit beseitigt.

## Ergebnis

- Offline-/Live-Installer können wieder regulär erzeugt und veröffentlicht werden.

---

Diese Version behebt einen Pipeline-Fehler im Installer-Build und stellt die Release-Fähigkeit wieder her.
