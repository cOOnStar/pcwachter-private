# Release Notes v0.0.49

Release-Datum: 26.02.2026
Tag: `v0.0.49`

## KurzÃ¼berblick

- Installer zeigt keinen `$(APP_NAME)`-Platzhalter mehr im Titel oder in Metadaten.
- Deinstallation wurde erweitert: laufende PC-WÃ¤chter-Prozesse werden vorab beendet.
- Alte Installationsordner (inkl. Namensvarianten) werden vollstÃ¤ndig bereinigt.

## Installer

- NSIS-Setup verwendet fÃ¼r sichtbare Titel und Dateimetadaten jetzt konsistent `APP_TITLE`/Branding statt abhÃ¤ngigem `APP_NAME`.
- Registry-DisplayName und Finish-Text wurden auf den finalen App-Titel vereinheitlicht.

## Deinstallation

- Vor Uninstall werden zusÃ¤tzlich `UpdaterWorker.exe`, `AgentService.exe` und `PCW*.exe` beendet.
- Der Dienst `PCWaechter Agent Service` wird vor dem Entfernen gestoppt.
- Neben `$INSTDIR` werden alte Installationspfade in `Program Files` und `LocalAppData\Programs` rekursiv gelÃ¶scht.

---

Diese Version fokussiert auf sauberes Branding im Installer und eine robustere vollstÃ¤ndige Deinstallation.

