# Release Notes v0.0.32

Release-Datum: 25.02.2026
Tag: `v0.0.32`

## KurzÃ¼berblick

- Installer-UX im Live-Installer verbessert: wÃ¤hrend des Downloads wird nur noch das Download-Fenster angezeigt.
- Download-Bannertext im kleinen Fenster auf Deutsch prÃ¤zisiert.
- Alte Namens-Fallbacks wurden vollstÃ¤ndig entfernt; nur das neue `live/offline`-Schema bleibt aktiv.
- Deutsche Commit-Standards kÃ¶nnen jetzt dauerhaft per Hook + Template erzwungen werden.

## Installer und Update-Pfad

- NSIS-Bootstrapper blendet das Hauptfenster wÃ¤hrend `inetc::get` aus.
- Reihenfolge im Bootstrapper wurde stabilisiert, sodass das Download-UI vor dem Start des eigentlichen Installers korrekt freigegeben wird.
- Updater in der Desktop-App verwendet nur noch `PCWaechter_offline_installer.exe` (kein altes Label-Fallback).
- Bannertext im Download-Fenster:
  - `Lade die neueste Version von PCWÃ¤chter herunter...`

## Namensschema-Bereinigung

- Entfernt: alte Fallbacks auf frÃ¼here Dateinamen in
  - `apps/installer-bootstrapper/Program.cs`
  - `scripts/build-installer.ps1`
  - `.github/workflows/release-installer.yml`
- Beibehalten: ausschlieÃŸlich
  - `PCWaechter_live_installer.exe`
  - `PCWaechter_offline_installer.exe`

## Workflow fÃ¼r deutsche Commit-Messages

Neu hinzugefÃ¼gt:

- `.githooks/commit-msg`
- `.gitmessage-de.txt`
- `scripts/setup-git-de-commits.ps1`

Einmalig ausfÃ¼hren:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\setup-git-de-commits.ps1
```

Dadurch werden `core.hooksPath` und `commit.template` lokal gesetzt und englische Betreffzeilen standardmÃ¤ÃŸig blockiert (Override mÃ¶glich).

